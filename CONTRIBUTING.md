# Contributing to Trellis

Run `bun install`, `bun run check`, `bun run check:fixtures`, and
`bun run package:check` before proposing a change.

A shared rule must be objective, useful in more than one repository, and have one
clear replacement. Prefer a built-in Biome rule. Keep product-specific architecture
and exceptions in the consuming repository.
